import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.payHelping',
  appName: 'pay-helping',
  webDir: 'dist/pay-helping',
  bundledWebRuntime: false
};

export default config;
